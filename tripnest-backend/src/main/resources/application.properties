spring.application.name=tripnest-backend
server.tomcat.basedir=./.tomcat
server.servlet.session.persistent=false

# H2 In-Memory Database for local development
spring.datasource.url=jdbc:h2:mem:tripnest
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# H2 Console for debugging
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

spring.jpa.hibernate.ddl-auto=create

spring.jpa.show-sql=false

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect

jwt.secret=${JWT_SECRET:your-super-secret-jwt-key-for-local-development-only}
jwt.expiration=86400000

openweather.api.key=${OPENWEATHER_API_KEY:demo-key}
